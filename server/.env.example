PORT=4000
DATABASE_URL=postgresql://postgres:YOURPASSWORD@localhost:5432/crane_cms
JWT_SECRET=change-this-to-something-long-and-random
CLIENT_ORIGIN=http://localhost:5173

# Optional — leave blank to have invoice emails logged to the console instead of sent
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
